!!!
%html
  %head
    %meta{ "http-equiv" => "content-type", :content => "text/html;charset=UTF-8" }/
    %title= "S Y M B O L t o P R O C"
    = stylesheet_link_tag :defaults,    :cache => true
    = javascript_include_tag :defaults, :cache => true
    = javascript_auto_include_tags
    = behaveJS_bootstrap
  %body{ :onload => "prettyPrint()" }
    #wrapper
      #header
        = link_to image_tag("banner.png"), root_path
      #content
        - unless flash.empty?
          %p#flash= flash[:notice]
        = yield
      #pusher
    #footer 
      #footer_internals
        %p [ evansenter => gmail . com ]
    = analytics_code